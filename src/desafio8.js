// Desafio 8
/*
function formatPhoneNumber(telNumber) {
    if (telNumber.lenght !== 11) {
        return 'Array com tamanho incorreto.';
    }
    for (let index = 0; index <telNumber.lenght. index++) {
        if (telNumber[index] < 0 || telNumberr[index] > 9) {
            return 'não é possível gerar um número de telefone com esses valores'
        }
    }
}

module.exports = {
    formatPhoneNumber
}
*/